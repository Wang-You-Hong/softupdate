name: Discord 更新播報

on:
  release:
    types: [edited, published]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: SethCohen/github-releases-to-discord@v1
        with:
          webhook_url: ${{ secrets.DISCORD_WEBHOOK }}
          content: ${{ secrets.DISCORD_MENTION }}
          username: HaWaTool 更新
          color: "3447003"
